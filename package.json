{
  "name": "prairiepilotfpv.github.io",
  "version": "1.0.0",
  "description": "Use these optional keys in posts, projects, or features to integrate with the magazine-style homepage:",
  "main": "index.js",
  "scripts": {
    "build": "bundle exec jekyll build",
    "optimize": "jampack _site",
    "build:opt": "npm run build && npm run optimize",
    "pdf:gallery": "node scripts/build-gallery-instructions.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@divriots/jampack": "^0.33.1",
    "pdfkit": "^0.17.2"
  }
}
